{# src/whiteLabel/BackOfficeBundle/Resources/views/Homepage/index.html.twig #}
{% extends "whiteLabelBackOfficeBundle::layout_core.html.twig" %}

{% block stylesheets %}
    {{ parent() }}
    {% stylesheets filter='cssrewrite'
    'bundles/whitelabelbackoffice/css/homepage/index.css'
    %}
    <link rel="stylesheet" href="{{ asset_url }}" type="text/css" />
    {% endstylesheets %}
{% endblock %}

{% block contents %}
<div class="col-xs-12 col-sm-12 col-md-10 col-lg-10 col-xs-offset-0 col-sm-offset-0 col-md-offset-1 col-lg-offset-1 hidden" id="content-wrapper">
    <h2 class="text-center">Menu</h2>

    {#
    <ul>
        {% for role in app.user.roles %}
        <li>{{ role }}</li>
        {% endfor %}
    </ul>

    {% for role in app.user.roles %}
        {{ role }}{% if false == loop.last %}, {% endif %}
    {% endfor %}

    {% if is_granted('ROLE_SUPER_ADMIN') %}
    <p>Je suis super-admin</p>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
    <p>Je suis admin</p>
    {% endif %}

    {% if is_granted('ROLE_CLIENT') %}
    <p>Je suis client</p>
    {% endif %}

    {% if is_granted('ROLE_AUDITEUR') %}
    <p>Je suis auditeur</p>
    {% endif %}

    {% if is_granted('ROLE_INSTRUCTEUR') %}
    <p>Je suis instructeur</p>
    {% endif %}

    {% if is_granted('ROLE_CONSEILLER') %}
    <p>Je suis conseiller</p>
    {% endif %}
    #}
</div>
{% endblock %}
